version: "2"

linters:
  default: standard

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-issues-per-linter: 50
  max-same-issues: 10

output:
  formats:
    text:
      path: stdout
  sort-order:
    - linter
    - file
